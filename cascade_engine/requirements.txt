# Core engine (legacy + fast paths)
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0
networkx>=2.6

# High-performance sparse engine
numba>=0.58.0          # JIT-compiled inner kernel (@njit parallel=True)
llvmlite>=0.41.0       # Numba's LLVM backend (auto-installed with numba)

# Caching (graph serialization)
joblib>=1.3.0          # optional: joblib.Memory for function-level memoisation

# Visualisation / reporting (unchanged)
matplotlib>=3.5.0
seaborn>=0.11.0

# Development / testing
pytest>=7.0.0
tqdm>=4.65.0           # optional: make_tqdm_callback progress bars
